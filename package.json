{
	"name": "wg_lms",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"frontend"
	],
	"scripts": {
		"postinstall": "yarn --cwd frontend install",
		"dev": "yarn --cwd frontend dev",
		"build": "yarn --cwd frontend build"
	}
}
